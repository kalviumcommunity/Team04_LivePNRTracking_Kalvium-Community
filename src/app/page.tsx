/**
 * @file page.tsx
 * @description Main application landing page route component.
 * Renders the primary marketing/landing page.
 */

import LandingPage from "@/components/landing-page";

/**
 * Default Home Page route component.
 * Serves as the main index view of the application.
 */
export default function Home() {
  return <LandingPage />;
}
